# Lab4 第四题题解：EXPLAIN ANALYZE、谓词下推、投影下推与别名显示

## 可跳转目录

- [sec-0：最终目标](#sec-0)
- [sec-1：整体执行链路](#sec-1)
- [sec-2：核心数据结构](#sec-2)
- [sec-3：Parser 层：识别 EXPLAIN ANALYZE / JOIN ON / AS](#sec-3)
- [sec-4：Analyze 层：别名转真实表名](#sec-4)
- [sec-5：Planner 层：FilterPlan、JoinPlan、ProjectPlan](#sec-5)
- [sec-6：Executor 层：rows 实时统计](#sec-6)
- [sec-7：Portal 层：Plan 指针传给 Executor](#sec-7)
- [sec-8：ExecutionManager 层：打印计划树](#sec-8)
- [sec-9：别名显示规则](#sec-9)
- [sec-10：投影下推规则](#sec-10)
- [sec-11：关键测试用例](#sec-11)
- [sec-12：常见错误与原因](#sec-12)
- [sec-13：最终检查清单](#sec-13)

## 按文件跳转

| 文件 | 作用 | 跳到说明 |
|---|---|---|
| [src/parser/yacc.y](src/parser/yacc.y) | SQL 语法扩展 | [Parser 层](#sec-3) |
| [src/parser/lex.l](src/parser/lex.l) | 关键字识别 | [Parser 层](#sec-3) |
| [src/parser/ast.h](src/parser/ast.h) | AST 结构扩展 | [核心数据结构](#sec-2) |
| [src/analyze/analyze.cpp](src/analyze/analyze.cpp) | 语义分析、别名转真实表名 | [Analyze 层](#sec-4) |
| [src/analyze/analyze.h](src/analyze/analyze.h) | Analyze 函数声明 | [Analyze 层](#sec-4) |
| [src/optimizer/plan.h](src/optimizer/plan.h) | Plan 节点定义 | [核心数据结构](#sec-2) |
| [src/optimizer/planner.cpp](src/optimizer/planner.cpp) | 生成计划树、谓词/投影下推 | [Planner 层](#sec-5) |
| [src/portal.h](src/portal.h) | Plan 转 Executor | [Portal 层](#sec-7) |
| [src/execution/executor_seq_scan.h](src/execution/executor_seq_scan.h) | Scan rows / Filter rows 统计 | [Executor 层](#sec-6) |
| [src/execution/executor_projection.h](src/execution/executor_projection.h) | Project rows 统计 | [Executor 层](#sec-6) |
| [src/execution/executor_nestedloop_join.h](src/execution/executor_nestedloop_join.h) | Join rows 统计 | [Executor 层](#sec-6) |
| [src/execution/execution_manager.cpp](src/execution/execution_manager.cpp) | EXPLAIN ANALYZE 输出 | [ExecutionManager 层](#sec-8) |

---

## sec-0

### 0. 最终目标

导航：[目录](#可跳转目录) | [下一节](#sec-1)

Lab4 第四题的目标不是简单执行 SELECT，而是支持：

```sql
EXPLAIN ANALYZE SELECT ...
```

它需要真实执行查询，但不输出 SELECT 表格，而是输出执行计划树。

最终要支持类似：

```text
Project(columns=[c.name, o.order_id], rows=5)
	Join(tables=[customers, orders], condition=[c.customer_id=o.customer_id], rows=5)
		Project(columns=[c.customer_id, c.name], rows=3)
			Scan(table=customers, type=SeqScan, rows=3)
		Project(columns=[o.customer_id, o.order_id], rows=15)
			Scan(table=orders, type=SeqScan, rows=15)
```

以及：

```text
Project(columns=[*], rows=2)
	Join(tables=[customers, orders], condition=[c.customer_id=o.customer_id], rows=2)
		Scan(table=customers, type=SeqScan, rows=3)
		Filter(condition=[o.total_amount>1000], rows=6)
			Scan(table=orders, type=SeqScan, rows=15)
```

重点有四个：

```text
1. Parser 能识别 EXPLAIN ANALYZE、JOIN ON、AS、别名
2. Analyze 内部统一使用真实表名
3. Planner 能生成 Filter / Join / Project 计划树
4. Executor 执行时实时统计每个 Plan 节点 rows
```

---

## sec-1

### 1. 整体执行链路

导航：[目录](#可跳转目录) | [上一节](#sec-0) | [下一节](#sec-2)

以：

```sql
EXPLAIN ANALYZE SELECT c.name, o.order_id
FROM customers c
JOIN orders o ON c.customer_id = o.customer_id;
```

为例，整体流程是：

```text
SQL 字符串
  |
  v
Parser
  生成 ast::SelectStmt
  记录表、别名、ON 条件、WHERE 条件、是否 EXPLAIN ANALYZE
  |
  v
Analyze
  检查表/列是否存在
  c.name -> customers.name
  o.order_id -> orders.order_id
  c.customer_id=o.customer_id -> customers.customer_id=orders.customer_id
  同时保存 table_to_alias: customers->c, orders->o
  |
  v
Planner
  生成 Plan 树
  做谓词下推、投影下推
  |
  v
Portal
  把 Plan 树转成 Executor 树
  把 Plan 指针传给 Executor
  |
  v
Executor
  真实执行查询
  同步更新 Plan::rows_
  |
  v
ExecutionManager
  不输出 SELECT 结果
  打印 Plan 树
```

---

## sec-2

### 2. 核心数据结构

导航：[目录](#可跳转目录) | [上一节](#sec-1) | [下一节](#sec-3)

相关文件：[src/parser/ast.h](src/parser/ast.h)、[src/optimizer/plan.h](src/optimizer/plan.h)。

#### 2.1 TableRef

`TableRef` 表示 FROM 中的一张表及其别名。

```cpp
struct TableRef {
    std::string tab_name;
    std::string alias;
};
```

例如：

```sql
FROM customers c
```

表示为：

```text
tab_name = customers
alias    = c
```

#### 2.2 FromClause

`JOIN ... ON ...` 会同时产生表和条件，所以 `tableList` 不能只返回表名列表，还要返回 ON 条件。

```cpp
struct FromClause {
    std::vector<ast::TableRef> tables;
    std::vector<std::shared_ptr<ast::BinaryExpr>> conds;
};
```

#### 2.3 Condition

`Condition` 是 Analyze 之后的条件表达式。

```cpp
struct Condition {
    TabCol lhs_col;
    CompOp op;
    bool is_rhs_val;
    TabCol rhs_col;
    Value rhs_val;
};
```

它能表示两类条件：

```sql
o.total_amount > 1000
```

```text
lhs_col = orders.total_amount
op = OP_GT
is_rhs_val = true
rhs_val = 1000
```

以及：

```sql
c.customer_id = o.customer_id
```

```text
lhs_col = customers.customer_id
op = OP_EQ
is_rhs_val = false
rhs_col = orders.customer_id
```

#### 2.4 Plan::rows_

所有 Plan 节点基类都需要：

```cpp
size_t rows_ = 0;
```

执行时 Executor 会把真实行数写回对应 Plan 节点。

#### 2.5 DMLPlan::table_to_alias_

为了打印时恢复别名，`DMLPlan` 保存：

```cpp
std::map<std::string, std::string> table_to_alias_;
```

例如：

```text
customers -> c
orders    -> o
```

注意：内部执行仍然用真实表名，只有打印 `columns` 和 `condition` 时使用别名。

---

## sec-3

### 3. Parser 层：识别 EXPLAIN ANALYZE / JOIN ON / AS

导航：[目录](#可跳转目录) | [上一节](#sec-2) | [下一节](#sec-4)

相关文件：[src/parser/lex.l](src/parser/lex.l)、[src/parser/yacc.y](src/parser/yacc.y)、[src/parser/ast.h](src/parser/ast.h)。

#### 3.1 lex.l

需要识别关键字：

```lex
"EXPLAIN" { return EXPLAIN; }
"ANALYZE" { return ANALYZE; }
"ON"      { return ON; }
"AS"      { return AS; }
```

#### 3.2 yacc.y token

加入：

```yacc
%token EXPLAIN ANALYZE ON AS
```

#### 3.3 SELECT 规则

普通 SELECT 要合并 `JOIN ON` 条件和 `WHERE` 条件：

```yacc
| SELECT selector FROM tableList optWhereClause opt_order_clause
{
    auto conds = $4.conds;
    conds.insert(conds.end(), $5.begin(), $5.end());
    $$ = std::make_shared<SelectStmt>($2, $4.tables, conds, $6);
}
```

EXPLAIN ANALYZE SELECT 同理：

```yacc
| EXPLAIN ANALYZE SELECT selector FROM tableList optWhereClause opt_order_clause
{
    auto conds = $6.conds;
    conds.insert(conds.end(), $7.begin(), $7.end());
    auto stmt = std::make_shared<SelectStmt>($4, $6.tables, conds, $8);
    stmt->is_explain_analyze = true;
    $$ = stmt;
}
```

重点：

```text
不能只用 WHERE 条件。
必须把 tableList 里的 ON 条件也合并进去。
```

#### 3.4 tableList

`JOIN ... ON condition` 要把 ON 条件放进 FromClause：

```yacc
| tableList JOIN tableRef ON condition
{
    $1.tables.push_back($3);
    $1.conds.push_back($5);
    $$ = $1;
}
```

---

## sec-4

### 4. Analyze 层：别名转真实表名

导航：[目录](#可跳转目录) | [上一节](#sec-3) | [下一节](#sec-5)

相关文件：[src/analyze/analyze.cpp](src/analyze/analyze.cpp)、[src/analyze/analyze.h](src/analyze/analyze.h)。

Analyze 的职责：

```text
1. 检查表是否存在
2. 检查列是否存在
3. 自动补全列所属表
4. 把别名转换成真实表名
5. 保存真实表名到别名的映射，用于最后打印
```

例如：

```sql
SELECT c.name, o.order_id
FROM customers c
JOIN orders o ON c.customer_id = o.customer_id;
```

Parser 看到的是：

```text
c.name
o.order_id
c.customer_id = o.customer_id
```

Analyze 后内部统一变成：

```text
customers.name
orders.order_id
customers.customer_id = orders.customer_id
```

同时保存：

```text
table_to_alias:
  customers -> c
  orders    -> o
```

这样后面的 Planner / Executor 不需要区分别名，最后打印时再恢复别名。

---

## sec-5

### 5. Planner 层：FilterPlan、JoinPlan、ProjectPlan

导航：[目录](#可跳转目录) | [上一节](#sec-4) | [下一节](#sec-6)

相关文件：[src/optimizer/planner.cpp](src/optimizer/planner.cpp)、[src/optimizer/plan.h](src/optimizer/plan.h)。

Planner 负责把 Query 变成 Plan 树。

#### 5.1 谓词下推

如果条件只涉及单表：

```sql
o.total_amount > 1000
```

就下推到 orders 的 Scan 附近：

```text
Filter(condition=[o.total_amount>1000])
	Scan(table=orders)
```

如果条件涉及两张表：

```sql
c.customer_id = o.customer_id
```

就保留为 JoinPlan 的条件：

```text
Join(condition=[c.customer_id=o.customer_id])
```

#### 5.2 投影下推

非 `SELECT *` 时，每个 Scan 上方要包局部 Project。

例如：

```sql
SELECT c.name, o.order_id
FROM customers c
JOIN orders o ON c.customer_id = o.customer_id;
```

customers 后续需要：

```text
c.customer_id
c.name
```

orders 后续需要：

```text
o.customer_id
o.order_id
```

所以计划树是：

```text
Project(columns=[c.name, o.order_id])
	Join(condition=[c.customer_id=o.customer_id])
		Project(columns=[c.customer_id, c.name])
			Scan(table=customers)
		Project(columns=[o.customer_id, o.order_id])
			Scan(table=orders)
```

#### 5.3 SELECT * 不做局部 Project

如果是：

```sql
SELECT *
```

则不需要在 Scan 上方额外包 Project，因为所有列都需要保留。

---

## sec-6

### 6. Executor 层：rows 实时统计

导航：[目录](#可跳转目录) | [上一节](#sec-5) | [下一节](#sec-7)

相关文件：

- [src/execution/executor_seq_scan.h](src/execution/executor_seq_scan.h)
- [src/execution/executor_projection.h](src/execution/executor_projection.h)
- [src/execution/executor_nestedloop_join.h](src/execution/executor_nestedloop_join.h)


Scan rows 在真正读取原始记录时增加：

```cpp
scan_plan_->rows_++;
```

Filter rows 在记录通过条件时增加：

```cpp
filter_plan_->rows_++;
```
####  NestedLoopJoinExecutor

Join rows 在拼接后的记录满足 Join 条件并输出时增加：

```cpp
plan_->rows_++;
```

---

## sec-7

### 7. Portal 层：Plan 指针传给 Executor

导航：[目录](#可跳转目录) | [上一节](#sec-6) | [下一节](#sec-8)

相关文件：[src/portal.h](src/portal.h)。

Portal 把 Plan 转换成 Executor。

为了让 Executor 更新 Plan::rows_，创建 Executor 时必须把对应 Plan 指针传进去。

#### 7.1 FilterPlan 不创建 FilterExecutor

FilterPlan 主要用于打印和 rows 统计。

实际过滤仍由 ScanExecutor 做。

所以：

```cpp
else if(auto x = std::dynamic_pointer_cast<FilterPlan>(plan)) {
    return convert_plan_executor(x->subplan_, context, x.get());
}
```

意思是：

```text
FilterPlan 不生成独立 Executor。
把 FilterPlan* 传给下面的 SeqScanExecutor。
```


## sec-8

### 8. ExecutionManager 层：打印计划树

导航：[目录](#可跳转目录) | [上一节](#sec-7) | [下一节](#sec-9)

相关文件：[src/execution/execution_manager.cpp](src/execution/execution_manager.cpp)。

`EXPLAIN ANALYZE` 的执行逻辑是：

```text
1. 清空 Plan 树 rows_
2. 执行 Executor 树
3. 丢弃 SELECT 结果 tuple
4. 打印 Plan 树
```

核心逻辑：

```cpp
reset_plan_rows(root_plan);

for (executorTreeRoot->beginTuple();
     !executorTreeRoot->is_end();
     executorTreeRoot->nextTuple()) {
    auto tuple = executorTreeRoot->Next();
}

append_plan_tree(oss, root_plan, 0, dml->table_to_alias_);
```

---

## sec-9

### 9. 别名显示规则

导航：[目录](#可跳转目录) | [上一节](#sec-8) | [下一节](#sec-10)

实验期望的别名规则是：

```text
Join(tables=[...])      使用真实表名
Scan(table=...)         使用真实表名
Project(columns=[...])  使用别名
Filter(condition=[...]) 使用别名
Join(condition=[...])   使用别名
```

例如：

```sql
FROM customers c JOIN orders o
```

打印：

```text
Join(tables=[customers, orders], condition=[c.customer_id=o.customer_id])
Scan(table=customers)
Filter(condition=[o.total_amount>1000])
Project(columns=[c.name, o.order_id])
```

实现方式：

```text
Analyze 阶段保存 table_to_alias
DMLPlan 保存 table_to_alias_
ExecutionManager 打印列和条件时使用 table_to_alias_
```

---

## sec-10

### 10. 投影下推规则

导航：[目录](#可跳转目录) | [上一节](#sec-9) | [下一节](#sec-11)

投影下推的核心是：每张表只保留后续还需要的列。

需要保留的列来自：

```text
1. SELECT 输出列
2. WHERE 条件列
3. JOIN 条件列
```

例如：

```sql
SELECT c.name, o.order_id
FROM customers c
JOIN orders o ON c.customer_id = o.customer_id;
```

需要：

```text
customers: customer_id, name
orders: customer_id, order_id
```

所以局部 Project 是：

```text
Project(columns=[c.customer_id, c.name])
Project(columns=[o.customer_id, o.order_id])
```

注意：`SELECT *` 不做局部 Project。

---


编译：

```bash
cd /mnt/hgfs/DMS-DESIGN/db2026
cmake --build build
```

运行服务端：

```bash
./build/bin/rmdb lab4_test_db
```

运行客户端：

```bash
cd /mnt/hgfs/DMS-DESIGN/db2026/rmdb_client
./build/rmdb_client
```

