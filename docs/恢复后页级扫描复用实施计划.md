# 恢复后页级扫描复用实施计划

**目标：** 为顺序扫描增加执行器局部的记录缓冲池，消除大表聚合扫描中重复的逐行堆分配，同时保持 SQL、MVCC、WAL、COMMIT、恢复和 Wire v3 语义不变。

**架构：** 新增一个只管理拥有型 `RmRecord` 的轻量记录池。`RmFileHandle::batch_get_records()` 可选地从记录池取得同尺寸缓冲区，`SeqScanExecutor` 负责在记录不再被上层引用后将其归还；页面读取、可见性过滤、谓词判断和聚合接口保持原顺序。

**技术栈：** C++17、现有 RMDB record/execution 模块、CMake/CTest。

## 全局约束

- 工作目录固定为 `D:\DMS-DESIGN\db2026\.worktrees\finals-wire-ssi`，不修改 `db2026` 根工作树。
- 不修改 Wire v3 协议、SQL 测试内容、TPC-C workload、MVCC/WAL、COMMIT 持久性和崩溃恢复语义。
- 不绕过 BufferPoolManager，不持有跨页的非拥有记录指针，不增加全局共享缓存。
- 保持现有 `commit_apply_latch_`、页面访问和 MVCC 锁顺序。
- 先写失败测试，再写最小实现；每个任务单独检查和提交。
- 本地验证不能替代平台 2500 万行数据复测，不把小规模测试描述成平台超时已经修复。

## 文件结构

- 新建 `src/record/rm_record_pool.h`：只负责取得和回收拥有型、定长的 `RmRecord`。
- 修改 `src/record/rm_file_handle.h`：为批量读取声明可选记录池参数。
- 修改 `src/record/rm_file_handle.cpp`：批量读取优先复用同尺寸记录缓冲区。
- 修改 `src/execution/executor_seq_scan.h`：持有局部记录池并在安全的执行器生命周期边界回收记录。
- 新建 `src/test/record_buffer_reuse_test.cpp`：验证同尺寸复用、内容覆盖和批量读取结果。
- 修改 `src/CMakeLists.txt`：注册聚焦测试 `test_record_buffer_reuse`。

---

### 任务 1：定义记录缓冲池契约

**文件：**

- 新建：`src/record/rm_record_pool.h`
- 新建：`src/test/record_buffer_reuse_test.cpp`
- 修改：`src/CMakeLists.txt`

**接口：**

- 产出：`RmRecordPool::acquire(int record_size) -> std::unique_ptr<RmRecord>`
- 产出：`RmRecordPool::release(std::unique_ptr<RmRecord> record) -> void`
- 产出：`RmRecordPool::available() const -> size_t`

- [ ] **步骤 1：先注册并编写失败测试**

在 `src/CMakeLists.txt` 增加：

```cmake
add_executable(test_record_buffer_reuse test/record_buffer_reuse_test.cpp)
target_link_libraries(test_record_buffer_reuse record system transaction storage pthread)
add_test(NAME test_record_buffer_reuse COMMAND test_record_buffer_reuse)
```

创建 `src/test/record_buffer_reuse_test.cpp`，先验证记录池最小契约：

```cpp
#include <cstdlib>
#include <cstring>
#include <iostream>
#include <memory>

#include "record/rm_record_pool.h"

namespace {

void require(bool condition, const char *message) {
    if (!condition) {
        std::cerr << "FAIL: " << message << '\n';
        std::exit(1);
    }
}

void require_same_size_record_is_reused() {
    RmRecordPool pool;
    auto first = pool.acquire(sizeof(int));
    RmRecord *object_address = first.get();
    char *data_address = first->data;
    int old_value = 17;
    std::memcpy(first->data, &old_value, sizeof(old_value));

    pool.release(std::move(first));
    require(pool.available() == 1, "released record must enter the pool");

    auto reused = pool.acquire(sizeof(int));
    require(reused.get() == object_address,
            "same-size acquire must reuse the record object");
    require(reused->data == data_address,
            "same-size acquire must reuse the record data buffer");
    require(pool.available() == 0, "acquire must remove the record from the pool");
}

void require_wrong_size_record_is_not_returned() {
    RmRecordPool pool;
    pool.release(std::make_unique<RmRecord>(sizeof(int) * 2));
    auto record = pool.acquire(sizeof(int));
    require(record->size == static_cast<int>(sizeof(int)),
            "acquire must return the requested record size");
}

}  // namespace

int main() {
    require_same_size_record_is_reused();
    require_wrong_size_record_is_not_returned();
    std::cout << "record buffer reuse tests passed\n";
    return 0;
}
```

- [ ] **步骤 2：构建测试并确认失败原因正确**

运行：

```bash
cmake --build build --target test_record_buffer_reuse -j2
```

预期：编译失败，错误明确指向缺少 `record/rm_record_pool.h` 或 `RmRecordPool`，而不是无关的链接错误。

- [ ] **步骤 3：实现最小记录池**

创建 `src/record/rm_record_pool.h`：

```cpp
#pragma once

#include <cstddef>
#include <memory>
#include <vector>

#include "record/rm_defs.h"

class RmRecordPool {
   public:
    std::unique_ptr<RmRecord> acquire(int record_size) {
        while (!records_.empty()) {
            auto record = std::move(records_.back());
            records_.pop_back();
            if (record != nullptr && record->allocated_ &&
                record->data != nullptr && record->size == record_size) {
                return record;
            }
        }
        return std::make_unique<RmRecord>(record_size);
    }

    void release(std::unique_ptr<RmRecord> record) {
        if (record != nullptr) {
            records_.push_back(std::move(record));
        }
    }

    size_t available() const { return records_.size(); }

   private:
    std::vector<std::unique_ptr<RmRecord>> records_;
};
```

- [ ] **步骤 4：运行聚焦测试**

运行：

```bash
cmake --build build --target test_record_buffer_reuse -j2
ctest --test-dir build -R '^test_record_buffer_reuse$' --output-on-failure
```

预期：`test_record_buffer_reuse` 通过。

- [ ] **步骤 5：提交任务 1**

```bash
git add src/record/rm_record_pool.h src/test/record_buffer_reuse_test.cpp src/CMakeLists.txt
git commit -m "Add reusable record buffer pool"
```

---

### 任务 2：让批量记录读取复用缓冲区

**文件：**

- 修改：`src/record/rm_file_handle.h`
- 修改：`src/record/rm_file_handle.cpp`
- 修改：`src/test/record_buffer_reuse_test.cpp`

**接口：**

- 消费：任务 1 的 `RmRecordPool::acquire()`。
- 产出：`RmFileHandle::batch_get_records(int, std::vector<Rid> &, Context *, RmRecordPool * = nullptr)`。
- 兼容：现有三个参数调用不需要修改。

- [ ] **步骤 1：扩展测试，先调用尚不存在的批量复用接口**

在测试中增加 `DiskManager`、`BufferPoolManager` 和 `RmManager`，写入三条整数记录，第一次批量读取后把结果归还池，更新底层记录，再次批量读取：

```cpp
void require_batch_read_reuses_buffers_and_overwrites_contents() {
    const std::string file_name = "record_buffer_reuse_test.db";
    DiskManager disk_manager;
    BufferPoolManager buffer_pool_manager(16, &disk_manager);
    RmManager rm_manager(&disk_manager, &buffer_pool_manager);
    if (disk_manager.is_file(file_name)) {
        disk_manager.destroy_file(file_name);
    }
    rm_manager.create_file(file_name, sizeof(int));
    auto file = rm_manager.open_file(file_name);

    std::vector<Rid> rids;
    for (int value : {1, 2, 3}) {
        rids.push_back(file->insert_record(
            reinterpret_cast<char *>(&value), nullptr));
    }
    std::vector<Rid> first_rids = rids;
    auto first = file->batch_get_records(rids.front().page_no,
                                         first_rids, nullptr);
    std::set<char *> buffers;
    RmRecordPool pool;
    for (auto &record : first) {
        buffers.insert(record->data);
        pool.release(std::move(record));
    }

    for (size_t i = 0; i < rids.size(); ++i) {
        int value = static_cast<int>(101 + i);
        file->update_record(rids[i], reinterpret_cast<char *>(&value), nullptr);
    }

    std::vector<Rid> second_rids = rids;
    auto second = file->batch_get_records(rids.front().page_no,
                                          second_rids, nullptr, &pool);
    require(second.size() == rids.size(), "batch read must retain every RID");
    require(pool.available() == 0, "batch read must consume reusable buffers");
    for (size_t i = 0; i < second.size(); ++i) {
        int value = 0;
        std::memcpy(&value, second[i]->data, sizeof(value));
        require(value == static_cast<int>(101 + i),
                "reused buffer must contain the latest page contents");
        require(buffers.count(second[i]->data) == 1,
                "batch read must reuse an available same-size buffer");
    }

    rm_manager.close_file(file.get());
    rm_manager.destroy_file(file_name);
}
```

测试文件同时增加：

```cpp
#include <set>
#include <string>
#include <vector>

#include "record/rm.h"
#include "storage/buffer_pool_manager.h"
#include "storage/disk_manager.h"
```

并在 `main()` 调用 `require_batch_read_reuses_buffers_and_overwrites_contents()`。

- [ ] **步骤 2：构建测试并确认接口缺失导致失败**

运行：

```bash
cmake --build build --target test_record_buffer_reuse -j2
```

预期：编译失败，指出 `batch_get_records` 不接受第四个参数。

- [ ] **步骤 3：增加兼容的批量读取签名**

在 `src/record/rm_file_handle.h` 引入记录池并修改声明：

```cpp
#include "rm_record_pool.h"

std::vector<std::unique_ptr<RmRecord>> batch_get_records(
    int page_no, std::vector<Rid> &rids, Context *context,
    RmRecordPool *record_pool = nullptr) const;
```

在 `src/record/rm_file_handle.cpp` 保持锁、取页、复制、解页和 MVCC 过滤顺序，只替换记录创建：

```cpp
std::vector<std::unique_ptr<RmRecord>> RmFileHandle::batch_get_records(
    int page_no, std::vector<Rid> &rids, Context *context,
    RmRecordPool *record_pool) const {
    std::vector<std::unique_ptr<RmRecord>> records;
    if (rids.empty()) {
        return records;
    }

    std::unique_lock<std::mutex> commit_apply_guard;
    if (context != nullptr && context->txn_mgr_ != nullptr) {
        commit_apply_guard = context->txn_mgr_->acquire_commit_apply_latch();
    }

    RmPageHandle page_handle = fetch_page_handle(page_no);
    std::vector<Rid> valid_rids;
    valid_rids.reserve(rids.size());
    records.reserve(rids.size());
    for (const auto &rid : rids) {
        if (rid.page_no != page_no ||
            !Bitmap::is_set(page_handle.bitmap, rid.slot_no)) {
            continue;
        }
        auto record = record_pool == nullptr
                          ? std::make_unique<RmRecord>(file_hdr_.record_size)
                          : record_pool->acquire(file_hdr_.record_size);
        memcpy(record->data, page_handle.get_slot(rid.slot_no),
               file_hdr_.record_size);
        records.push_back(std::move(record));
        valid_rids.push_back(rid);
    }
    buffer_pool_manager_->unpin_page(PageId{fd_, page_no}, false);

    if (context != nullptr && context->txn_mgr_ != nullptr) {
        context->txn_mgr_->filter_visible_records(
            context->txn_, mvcc_file_id_, valid_rids, records);
    }

    rids = std::move(valid_rids);
    return records;
}
```

- [ ] **步骤 4：运行记录复用与可见性测试**

运行：

```bash
cmake --build build --target test_record_buffer_reuse test_batch_visibility -j2
ctest --test-dir build -R '^(test_record_buffer_reuse|test_batch_visibility)$' --output-on-failure
```

预期：两个测试均通过。

- [ ] **步骤 5：提交任务 2**

```bash
git add src/record/rm_file_handle.h src/record/rm_file_handle.cpp src/test/record_buffer_reuse_test.cpp
git commit -m "Reuse buffers during batch record reads"
```

---

### 任务 3：接入顺序扫描的安全回收边界

**文件：**

- 修改：`src/execution/executor_seq_scan.h`
- 修改：`src/test/aggregate_distinct_test.cpp`

**接口：**

- 消费：任务 2 的可选 `RmRecordPool *` 参数。
- 保持：`AbstractExecutor::current_record()` 指针在下一次 `nextTuple()` 前有效。
- 保持：`Next()` 仍返回独立拥有的记录副本。

- [ ] **步骤 1：增加源码契约测试，锁定接入点和禁止项**

在 `src/test/aggregate_distinct_test.cpp` 增加读取源码的契约检查，要求 `SeqScanExecutor` 拥有局部池、批量读取传入池，并明确不存在全局静态池：

```cpp
#include <fstream>
#include <iterator>
#include <string>

void require_seq_scan_owns_local_record_pool(const std::string &source_root) {
    std::ifstream input(source_root + "/src/execution/executor_seq_scan.h");
    std::string source((std::istreambuf_iterator<char>(input)),
                       std::istreambuf_iterator<char>());
    require(source.find("RmRecordPool record_pool_") != std::string::npos,
            "SeqScanExecutor must own a local record pool");
    require(source.find("context_, &record_pool_") != std::string::npos,
            "page batch reads must consume the local record pool");
    require(source.find("static RmRecordPool") == std::string::npos,
            "record pools must not be global or shared across scans");
}
```

将测试程序的 `main` 改为接收源码根目录并调用该检查：

```cpp
int main(int argc, char **argv) {
    require(argc == 2, "aggregate test requires the source root");
    require(run_count(false) == 7, "COUNT(col) must retain every input row");
    require(run_count(true) == 3,
            "COUNT(DISTINCT col) must eliminate join fan-out duplicates");
    require(run_float_sum({16777216.0F, 1.0F, 1.0F}) == 16777218.0F,
            "SUM(FLOAT) must accumulate binary32 inputs in binary64 and "
            "round once");
    require_aggregate_borrows_scan_records();
    require_seq_scan_owns_local_record_pool(argv[1]);
    std::cout << "aggregate distinct tests passed\n";
    return 0;
}
```

在 `src/CMakeLists.txt` 中把测试命令改成：

```cmake
add_test(NAME test_aggregate_distinct
         COMMAND test_aggregate_distinct ${CMAKE_SOURCE_DIR})
```

- [ ] **步骤 2：运行测试并确认缺少扫描器记录池导致失败**

运行：

```bash
cmake --build build --target test_aggregate_distinct -j2
ctest --test-dir build -R '^test_aggregate_distinct$' --output-on-failure
```

预期：测试失败，信息为 `SeqScanExecutor must own a local record pool`。

- [ ] **步骤 3：实现扫描器局部回收**

在 `SeqScanExecutor` 的批次成员附近增加：

```cpp
RmRecordPool record_pool_;
```

增加两个私有辅助函数：

```cpp
void recycle_record(std::unique_ptr<RmRecord> &record) {
    record_pool_.release(std::move(record));
}

void recycle_batch_records() {
    recycle_record(current_rec_);
    for (auto &record : batch_recs_) {
        recycle_record(record);
    }
    batch_recs_.clear();
    batch_rids_.clear();
    batch_index_ = 0;
}
```

把 `load_next_page_batch()` 开头的直接清空替换为：

```cpp
recycle_batch_records();
```

批量读取传入局部池：

```cpp
page_recs = fh_->batch_get_records(
    page_no, page_rids, context_, &record_pool_);
```

谓词过滤失败的记录立即回收：

```cpp
for (size_t i = 0; i < page_recs.size(); ++i) {
    if (scan_plan_ != nullptr) {
        scan_plan_->rows_++;
    }
    if (fed_conds_.empty() ||
        eval_conditions(*page_recs[i], fed_conds_, cols_)) {
        if (filter_plan_ != nullptr) {
            filter_plan_->rows_++;
        }
        if (track_serializable_reads_ && context_->txn_mgr_ != nullptr) {
            context_->txn_mgr_->register_record_read(
                context_->txn_, fh_->GetMvccFileId(), page_rids[i]);
        }
        batch_recs_.push_back(std::move(page_recs[i]));
        batch_rids_.push_back(page_rids[i]);
    } else {
        recycle_record(page_recs[i]);
    }
}
```

`beginTuple()` 开头使用 `recycle_batch_records()`，不再直接清空 `batch_recs_`、`batch_rids_` 和 `current_rec_`。

在同一批次内前进时，先回收上一个当前记录，再移动下一个记录：

```cpp
if (batch_index_ + 1 < batch_recs_.size()) {
    recycle_record(current_rec_);
    batch_index_++;
    current_rec_ = std::move(batch_recs_[batch_index_]);
    rid_ = batch_rids_[batch_index_];
    return;
}
```

等值缓存路径继续使用原有 `get_record()`，不为非热点路径扩大接口。

- [ ] **步骤 4：运行聚合、记录复用和可见性测试**

运行：

```bash
cmake --build build --target test_aggregate_distinct test_record_buffer_reuse test_batch_visibility -j2
ctest --test-dir build -R '^(test_aggregate_distinct|test_record_buffer_reuse|test_batch_visibility)$' --output-on-failure
```

预期：三个测试均通过，聚合仍借用当前记录而不调用 `Next()` 复制每一行。

- [ ] **步骤 5：提交任务 3**

```bash
git add src/execution/executor_seq_scan.h src/test/aggregate_distinct_test.cpp src/CMakeLists.txt
git commit -m "Recycle sequential scan record buffers"
```

---

### 任务 4：完成回归和差异审计

**文件：**

- 检查：`src/record/rm_record_pool.h`
- 检查：`src/record/rm_file_handle.h`
- 检查：`src/record/rm_file_handle.cpp`
- 检查：`src/execution/executor_seq_scan.h`
- 检查：`src/test/record_buffer_reuse_test.cpp`
- 检查：`src/test/aggregate_distinct_test.cpp`
- 检查：`src/CMakeLists.txt`

**接口：** 不新增行为，只验证前三个任务的组合结果。

- [ ] **步骤 1：构建全部已有 CTest 目标**

```bash
cmake --build build -j2
```

预期：构建成功且没有新增编译警告。

- [ ] **步骤 2：运行 CTest 回归**

```bash
ctest --test-dir build --output-on-failure
```

预期：全部测试通过，包括 `test_parser`、Wire、聚合、批量可见性和记录复用测试。

- [ ] **步骤 3：检查补丁卫生和范围**

```bash
git diff --check HEAD~3
git status --short
git diff HEAD~3 -- src/record src/execution/executor_seq_scan.h src/test src/CMakeLists.txt
```

预期：没有空白错误；没有 build、数据库目录、日志或测试数据进入 Git；差异不涉及 network、transaction、recovery 或 SQL workload。

- [ ] **步骤 4：记录验证结论**

最终交付中明确列出实际运行命令和结果，并注明未运行完整 2500 万行平台数据时，恢复后超时仍需平台复测确认。若任一测试失败，不提交“已修复”结论。
