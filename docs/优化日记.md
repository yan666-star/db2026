
## 优化记录摘要

历史优化过程已压缩，只保留对后续性能工作仍有价值的结论：

- MVCC 热路径：唯一键冲突检查只关注发生过 MVCC 写入的候选 key；非索引列 UPDATE 不做无意义的唯一键检查和索引维护。
- 读路径：优化缓冲池命中、唯一键点查、索引范围扫描与叶页读取，目标是缩短事务执行时间和冲突窗口。
- abort 成本：保留 `RMDB_PERF_DIAG` 诊断；纯内存 MVCC abort 可避免不必要的 checkpoint flush；pending writer 采用有限等待，最终仍按 SI 冲突规则决定提交或 abort。
- 保持边界：不放松 MVCC 可见性、写冲突、WAL、COMMIT/ABORT 顺序、redo/undo、checkpoint 或表/索引/版本链同步维护。

## 回归验证

每次优化至少验证官方性能脚本的功能、Load Data、压测后 TPC-C 一致性和 `kill -9` 恢复阶段；本地重点覆盖并发 NewOrder 守恒、失败 NewOrder 无残留、扫描与索引一致、未提交写不可见，以及 abort 不覆盖已提交更新。

## 2026-07-11 Task 1：负载校准与阶段诊断

- benchmark 新增 `official-shape` 和 `mini-contention` 两种明确模式。
- `official-shape` 保证同一订单 item 不重复，并用一条多列 UPDATE 同时修改
  `s_quantity/s_ytd/s_order_cnt`；`mini-contention` 保留旧版重复 item 和三条
  UPDATE，用作最坏冲突压力测试。
- JSON 现在记录数据规模、SQL 形状、请求隔离、实际隔离来源，以及按阶段计算的
  `RMDB_PERF_DIAG` 观测差值。
- 当前 `default` worker 在 `set output_file off` 后由新连接继承 SI，JSON 已明确记录，
  不再把它误报成普通默认 RC。

16 客户端 quick 结果：

| 模式 | tpmC | TPS | 提交率 | 正确性与恢复 |
| --- | ---: | ---: | ---: | --- |
| official-shape | 605.25 | 139.61 | 13.06% | PASS |
| mini-contention | 485.88 | 135.67 | 12.34% | PASS |

通用 ACID、SI 幻读、唯一键冲突、压测后一致性、order-line 基线差值和
`kill -9` 恢复全部通过。

## 2026-07-11 Task 2：volatile-only MVCC abort 免强制刷盘

严格限定的 `volatile_only_mvcc_abort` 同时满足：

1. 事务使用 MVCC；
2. 没有执行物理 rollback；
3. 没有进入 `commit_mvcc` 物理应用阶段。

这类事务仍把 ABORT record 追加到有序日志缓冲区，但不再单独调用
`flush_log_to_disk(true)`。非 MVCC abort、INSERT 等物理回滚和进入 commit
应用后的 abort 保留原来的 checkpoint flush 与强制日志刷盘路径。

新增恢复覆盖：纯 UPDATE/DELETE rollback、ABORT 被后续提交日志带出后崩溃、
ABORT 后立即 `kill -9`。原有 INSERT rollback 和已提交事务持久性检查继续保留。

与 Task 1 相同的 16 客户端 official-shape quick 对比：

| 指标 | Task 1 | Task 2 | 变化 |
| --- | ---: | ---: | ---: |
| NewOrder tpmC | 605.25 | 784.98 | +29.7% |
| TPS | 139.61 | 173.44 | +24.2% |
| 总提交率 | 13.06% | 13.83% | +0.77 pp |
| 平均延迟 | 14.93 ms | 12.73 ms | -14.8% |
| ABORT 强制刷盘次数 | 7500 | 686 | -90.9% |
| ABORT 强制刷盘耗时 | 4.29 s | 0.52 s | -87.9% |
| 跳过 ABORT 强制刷盘 | 0 | 8007 | 新增 |

Task 2 中 `abort_checkpoint_flush=682`、`abort_physical_rollback=682`，说明需要
物理回滚的事务仍走耐久路径，没有把所有 abort 无条件改成免刷盘。所有通用
ACID、性能 smoke、压测后一致性和崩溃恢复命令均成功结束。

## 2026-07-11 Task 3：pending writer 等待预算

新增诊断把 resolved wait 拆分为 writer committed、writer aborted 和 writer
state gone；benchmark 可通过 `--pending-wait-us` 对同一负载做可复现 A/B。

16 客户端、official-shape、三轮 2 秒预热/8 秒测量结果：

| 等待预算 | tpmC 中位数 | TPS 中位数 | 提交率 | 平均延迟 |
| ---: | ---: | ---: | ---: | ---: |
| 0 us | 680.28 | 191.95 | 12.73% | 10.69 ms |
| 50 us | 686.95 | 183.55 | 12.78% | 11.11 ms |
| 200 us | 724.68 | 186.02 | 13.12% | 11.25 ms |
| 500 us | 687.08 | 174.10 | 13.32% | 11.75 ms |
| 2000 us | **829.54** | 172.33 | **13.98%** | 13.10 ms |

2000 us 下共观测 26,706 次等待：4,967 次等到 writer abort，2,326 次等到
writer commit，1 次 writer state gone，19,411 次 timeout。在已解决等待中，
约 68.1% 等到 writer abort，因此等待确实能挽救部分 SI 事务；0 us 虽然总 TPS
较高，但成功 NewOrder tpmC 明显更低。

保留现有默认 2000 us，不增加线程 sleep、全局串行或隐藏重试。最终通用 ACID、
volatile abort 两种崩溃时机、已提交持久性、压测后一致性及 benchmark `kill -9`
全部通过；最终 quick tpmC 为 821.50。

## 2026-07-11 Task 4a：BufferPool 页表与 frame 锁拆分

诊断基线中 BufferPool 全局 latch 累计等待 22.08 秒，平均 10.10 us，明显高于
commit-apply read 的 7.72 秒和 MVCC latch 的 1.80 秒；命中率约为 100%，因此
优化目标是命中/取消固定并发，而不是磁盘 miss。

实施通用页表共享锁和 per-frame 状态锁：

- fetch hit、unpin 和单页 flush 持有页表共享锁。
- miss、装页、淘汰、删除和全量 flush 持有页表独占锁。
- 每个 frame 独立保护 `pin_count/is_dirty`。
- `pin_count 0→1` 与 LRU 移除、`1→0` 与 LRU 加入仍在同一个 frame 临界区。
- commit-apply 和 MVCC 锁在本阶段未修改。

16 客户端 official-shape quick 对比：

| 指标 | 修改前 | 修改后 | 变化 |
| --- | ---: | ---: | ---: |
| NewOrder tpmC | 829.36 | 866.88 | +4.5% |
| TPS | 167.24 | 176.74 | +5.7% |
| Buffer 页表总等待 | 22.08 s | 19.56 s | -11.4% |
| Buffer 页表平均等待 | 10.10 us | 8.00 us | -20.8% |
| per-frame 平均等待 | 未分离 | 0.052 us | 新增 |

完整 local quick、并发一致性、非法 item rollback、压测后一致性、order-line
基线差值和 `kill -9` 恢复全部通过。剩余页表等待与物理 abort 的 checkpoint
独占全量刷盘高度相关；该问题需要 CLR/恢复协议证明，暂不在低风险阶段处理。

### 未保留：commit-apply 公平读写 gate

曾将普通记录读取改为共享 read guard，并让物理 commit/GC/checkpoint 使用带
writer intent 的公平独占 guard。该实验把 commit-apply read 等待从 7.87 秒
降至 1.47 秒，但串行压力转移到 MVCC latch：MVCC 等待从 1.76 秒升至 3.12 秒。

| 指标 | BufferPool 版本 | 公平读写 gate | 变化 |
| --- | ---: | ---: | ---: |
| NewOrder tpmC | 866.88 | 859.22 | -0.9% |
| TPS | 176.74 | 174.58 | -1.2% |
| 平均延迟 | 12.82 ms | 13.10 ms | +2.2% |
| commit-apply read 等待 | 7.87 s | 1.47 s | -81.3% |
| MVCC latch 等待 | 1.76 s | 3.12 s | +77.4% |

该改动没有形成端到端收益，却增加了并发协议复杂度，因此已完整回退，只保留
诊断计数和已验证有收益的 BufferPool 锁拆分。后续不得仅凭某一把锁等待下降
就重复引入该方案；必须证明成功 tpmC/TPS 的整体提升。

## 2026-07-11 最终候选基线

BufferPool 优化保留、commit-apply 公平读写 gate 回退后的候选版本结果：

| 并发与时长 | NewOrder tpmC | 结果 |
| --- | ---: | --- |
| 16 clients，quick | 858.93 | 一致性、kill-9 恢复 PASS |
| 16 clients，10 秒预热 + 60 秒测量，3 轮 | **851.68**（中位数） | 三轮为 826.59 / 852.64 / 851.68，一致性、kill-9 恢复 PASS |
| 32 clients，quick | 418.51 | 一致性、kill-9 恢复 PASS |

32 clients 下成功 NewOrder 明显下降，说明剩余问题主要是高并发写冲突和串行热点，
不能以放松 SI 冲突检查、减少正确性刷盘或识别 TPC-C SQL 的方式换取分数。

## 2026-07-11 参考文档复核与 Task 5a：索引点查去临时堆分配

复核《决赛优化专题》和《设计与实现文档》后，当前版本已经具备哈希化 LRU、
批量记录读取、批量索引扫描等机制；下列方案暂不直接引入：

- `fast_count/fast_min`：必须同时证明当前事务的 MVCC 可见性，不能只依赖物理行数或索引首项；
- 改用较弱隔离级别或简化冲突检查：会改变现有 SI 语义；
- B+ 树 crab-latching、CLR/WAL 协议重构：可能有收益，但需要更大范围的并发与恢复证明；
- 针对固定表、字段或 SQL 的专用路径：明确禁止。

本阶段采用文档中的通用“减少构造/析构与内存分配”方向。原
`IxIndexHandle::get_value` 在每一层 B+ 树页面上都创建和销毁一个堆分配的
`IxNodeHandle`。修改后在共享根锁保护下使用栈上轻量句柄逐层读取，页面
fetch/unpin 顺序、键比较、锁范围和返回结果保持不变。该优化对任意 B+ 树唯一键点查生效，
不读取表名、字段名或 SQL 文本。

该候选必须与上面的 16-client 三轮基线进行 A/B；若 tpmC 中位数没有稳定收益，
则回退本阶段，不与后续优化合并。

### Task 5a 验收结果

完整 quick 门禁、一致性、order-line 基线差值和 kill-9 恢复全部通过。16 clients、
10 秒预热、60 秒测量、3 轮的 NewOrder tpmC 为 882.55 / 890.57 / 920.80，
中位数 890.57；相对修改前中位数 851.68 提升 4.57%。新版本最低一轮仍高于
旧版本最高一轮，因此保留索引点查去 `IxNodeHandle` 临时堆分配优化。

### 未保留：Task 5b 单 RID 直接读取

曾为完整等值索引查询增加单 RID 接口，绕过 `vector/map/batch` 中转。该版本所有
正确性门禁均通过，但端到端性能明显下降：

| 指标 | Task 5a | Task 5b | 变化 |
| --- | ---: | ---: | ---: |
| quick tpmC | 875.70 | 814.10 | -7.0% |
| 三轮 tpmC 中位数 | 890.57 | 821.51 | -7.75% |

Task 5b 三轮为 807.73 / 821.51 / 833.69，全部低于 Task 5a 三轮。说明单行查询
原有 batch 路径的表面容器开销并不是当前主导瓶颈，或者直接路径破坏了更有利的
执行局部性。该实验已完整回退，不能与后续改动合并，也不能仅凭减少对象数量判断收益。

### Task 5c 诊断与回退

曾在 `RMDB_PERF_DIAG` 中加入 parser 锁和 B+ 树根锁计时。诊断 JSON 有效，但首次
提取命令错误地读取了不存在的 `round["measure"]["tps"]`；正确字段是
`round["transactions_per_second"]`。该命令错误不属于数据库执行结果。

正式测量阶段诊断差值如下：

| 热点 | 累计等待 |
| --- | ---: |
| parser 全局锁 | 20.78 s |
| Buffer 页表锁 | 20.08 s |
| commit-apply read | 7.66 s |
| MVCC latch | 1.81 s |
| B+ 树根读锁 | 0.022 s |
| B+ 树根写锁 | 0.035 s |

因此 B+ 树根锁不是当前主热点。关闭诊断的 quick tpmC 只有 785.58，低于 Task 5a
的 875.70；为避免任何默认热路径成本，Task 5c 计时封装已完整回退，只保留本次
离线诊断结论。

### Task 5d 候选：缩短 parser 全局锁范围

生成的 lexer/parser 使用全局 `ast::parse_tree`，必须串行执行解析；但 AST 节点由
`shared_ptr` 和自有字符串/值组成。当前请求可在锁内复制根 `shared_ptr` 并释放 lexer
buffer，随后在锁外执行事务开始、语义分析、优化、portal 和执行。该修改不缓存、匹配
或改写固定 SQL，也不改变 AST 内容，只移除无关客户端之间的分析阶段串行等待。

本阶段仍需完整验证 RC/SI/Serializable、非法 item rollback、压测后一致性和 kill-9
恢复；性能以 Task 5a 的 890.57 三轮中位数为基线。

## 2026-07-12 Task 6：abort checkpoint single-flight 与诊断修正

### 背景

当前版本正确性已经通过，但 16 clients 的 quick 压测在 abort-heavy 场景下仍只有约
109.50 NewOrder tpmC：

| 指标 | 结果 |
| --- | ---: |
| warmup commits / aborts | 57 / 218 |
| measure commits / aborts | 192 / 1000 |
| measure TPS | 23.36 |
| NewOrder tpmC | 109.50 |

压测后 TPC-C 一致性、order-line baseline-gap、kill-9 durability marker、重启后一致性全部
PASS，说明本轮问题不是正确性回退，而是 abort 代价和诊断采样问题。

### 修改

- 对需要物理 rollback 的 abort 引入通用 single-flight/coalesced checkpoint：rollback 完成后发布 ticket，一个实际 checkpoint 可覆盖开始刷盘前已经发布的多个 ticket。
- 保持恢复顺序不变：物理 rollback 持久化完成后，才允许写入并强制刷出 ABORT log record。
- 新增 `RMDB_ABORT_CRASH_PAUSE_US` 测试钩子，专门覆盖“rollback checkpoint 已完成、ABORT record 尚未写入”窗口的 kill-9 恢复。
- 新增诊断字段：`abort_checkpoint_actual`、`abort_checkpoint_reused`、`abort_checkpoint_wait_us`，并保留 INSERT rollback/checkpoint 计时。

### 本次失败与修正

`check_abort_perf_diag_breakdown.py` 曾要求：

```text
abort_checkpoint_actual + abort_checkpoint_reused == abort_checkpoint_flush
```

这在并发采样下过强。`abort_checkpoint_flush` 是“发起 durable 请求”的计数，而
`actual/reused` 是“ensure 路径已完成”的计数；旧代码还在 abort 开头打印
`RMDB_PERF_DIAG`，日志可能正好采到“请求已登记、checkpoint 仍在进行”的中间态。

修正：

- 将 `maybe_print_perf_diag(abort_count)` 从 `TransactionManager::abort()` 开头移动到
  abort 完成后，减少中间态诊断。
- checker 不再把并发瞬时采样差值当作失败，而是输出 `checkpoint_sample_gap` 供判断
  single-flight 是否真的合并了 checkpoint。

### 提交前必须跑的验证

```bash
cmake --build build -j

python3 'SQL测试/performance_test/probe_crash_between_abort_checkpoint_and_log.py'

SKIP_BUILD=1 bash 'SQL测试/performance_test/run_local_evaluation.sh' quick \
  2>&1 | tee build/coalesced_abort_quick.log

rm -rf build/tpcc_abort_coalesced
python3 'SQL测试/performance_test/run_official_like_benchmark.py' \
  --start-server --quick --clients 16 --isolation snapshot \
  --pending-wait-us 10000 \
  --perf-diag --perf-diag-interval 100 \
  --crash-check \
  --db-name tpcc_abort_coalesced \
  --server-log build/tpcc_abort_coalesced.log \
  --json-output build/tpcc_abort_coalesced.json

python3 'SQL测试/performance_test/check_abort_perf_diag_breakdown.py' \
  build/tpcc_abort_coalesced.json
```

验收标准：所有一致性和 crash recovery 继续 PASS；checker 输出
`checkpoint_actual/checkpoint_reused/checkpoint_sample_gap`，不再因为并发采样瞬时差值误报失败。

### Task 6 实测结论

16 clients quick gate 结果如下：

| 指标 | 结果 |
| --- | ---: |
| warmup commits / aborts | 70 / 287 |
| measure commits / aborts | 186 / 1107 |
| measure TPS | 22.92 |
| NewOrder tpmC | 81.33 |
| 压测后 TPC-C consistency | PASS |
| order-line baseline-gap | PASS |
| kill-9 durability marker | PASS |
| 重启后 TPC-C consistency | PASS |

abort 诊断：

```text
insert=236, update=251, delete=0, insert_txns=80, max_write_set=8,
insert_rollback_us=419919, insert_checkpoint_flush_us=3965085,
checkpoint_actual=74, checkpoint_reused=6,
checkpoint_wait_us=1094983, checkpoint_sample_gap=-1
```

结论：single-flight 方案在正确性上成立，且 checker 不再因为并发采样误报失败；但端到端
NewOrder tpmC 从上一轮约 109.50 下降到 81.33，`checkpoint_reused=6` 说明实际合并收益很小，
`checkpoint_wait_us=1094983` 说明 coalescing latch 反而引入了明显等待。因此本方案不应作为当前
主线性能收益项提交；若要保留，只适合作为“可关闭的诊断/实验分支”。当前准备提交新版本时，
优先保留已经证明有收益且正确性稳定的优化，回退或禁用 Task 6 的 single-flight 运行时路径。

最终处理：已回退 Task 6 的 single-flight/coalesced checkpoint 运行时路径，恢复为物理
rollback 后直接 `flush_for_checkpoint()`。保留 abort 诊断字段和专项 crash 测试思路，用于后续
定位成本，但不再把 coalescing latch 放入主线性能版本。
